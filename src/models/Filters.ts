export interface PlayerFilters {
  playerId?: string;
  playerName?: string;
  userName?: string;
}

export interface SeasonFilters {
  startDate?: string;
  seasonId?: string;
  isActive?: boolean;
}
